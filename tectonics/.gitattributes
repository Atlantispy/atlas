# Explicit LF for this new source package; no old file is renormalised.
*.py text eol=lf
*.md text eol=lf
*.json text eol=lf
*.toml text eol=lf

# Preserve pinned reference bytes; Git must not normalise their line endings.
reference_data/** -text
